# Notas internas de trabajo — solo local, NO publicar en Vercel
CONTEXTO-LANDING.md

# Backups y basura de respaldos
*.bak
*.tmp
*.backup
*.clean

# Archivos sensibles — NUNCA subir (contraseñas, códigos 2FA, claves)
recovery-codes.txt
*recovery-code*
*.secret
.env
.env.*
